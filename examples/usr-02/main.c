#include <stdio.h>
#include <stdlib.h>
int x,y,z;
int main()
{
    printf("please enter first number then press:");
    scanf ("%d", &x);
    printf ("please enter second number then press:");
    scanf ("%d", &y);
    printf ("x=%d, y=%d, x+y=%d\n", x, y, z=x+y);

    printf("please enter first number then press:");
    scanf ("%d", &x);
    printf ("please enter second number then press:");
    scanf ("%d", &y);
    printf ("x=%d, y=%d, x-y=%d\n", x, y, z=x-y);

    printf("please enter first number then press:");
    scanf ("%d", &x);
    printf ("please enter second number then press:");
    scanf ("%d", &y);
    printf ("x=%d, y=%d, x*y=%d\n", x, y, z=x*y);

    printf("please enter first number then press:");
    scanf ("%d", &x);
    printf ("please enter second number then press:");
    scanf ("%d", &y);
    printf ("x=%d, y=%d, x/y=%d\n", x, y, z=x/y);
}
